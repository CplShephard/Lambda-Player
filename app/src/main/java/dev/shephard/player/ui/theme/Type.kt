package dev.shephard.player.ui.theme

import androidx.compose.material3.Typography
import androidx.compose.ui.text.font.FontFamily

// Bebas Neue için app/src/main/assets/bebas_neue.ttf eklenirse:
// val BrandFontFamily = FontFamily(Font("bebas_neue.ttf"))
// Şimdilik sistem sans-serif-condensed kullanılıyor (Roboto Condensed).
val BrandFontFamily = FontFamily.SansSerif

val AppTypography = Typography()
